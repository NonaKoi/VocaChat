# VocaChat Development Log
## 2026-07-13 — 第二阶段：核心业务与 Service 整理

- 完成 Model、Service、ConsoleUi 和程序入口的职责分离，核心账号、群聊、消息和假回复规则现在能够脱离 Console 直接调用和测试。
- `/done`、`/exit` 和成员编号保留在 ConsoleUi；业务 Service 负责最终验证；账号、群成员和消息通过只读集合公开，避免外部绕过业务规则修改数据。
- 当前仍以内存集合和本地假回复作为临时方案；内存集合将在持久化阶段替换，假回复将在真实模型接入阶段替换。
- 简单 `@昵称` 仍采用字符串匹配，昵称存在前缀重叠时可能选错成员，后续需要改为边界明确的点名解析。
- 下一阶段进入 SQLite 与 EF Core，优先实现现有账号、群聊、成员关系和消息的持久化

## 2026-07-13 — 第一阶段：Console 群聊原型

- 完成最小群聊链路：用户创建多个 AI 账号、选择已有账号加入群聊、发送用户消息、选择一个群内 AI 生成假回复，并在内存中保存和显示聊天记录。
- 内存保存和本地假回复作为验证业务链路的临时方案
- `Program.cs` 和早期 Service 的职责过多，下一阶段需要分离 ConsoleUi、整理业务 Service 并补充自动化测试。
